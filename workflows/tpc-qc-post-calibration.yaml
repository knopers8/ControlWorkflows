name: tpc-qc-post-calibration
vars:
  dpl_command: >-
    o2-qc --config {{ qc_config_uri }} --remote -b
defaults:
  qc_remote_machine: "alio2-cr1-qme01"
  qc_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/tpc-qc-post-calibration"
  monitoring_dpl_url: "no-op://"
  user: "flp"
  fmq_rate_logging: 0
  shm_segment_size: 10000000000
  shm_throw_bad_alloc: false
  session_id: default
  resources_monitoring: 15
constraints:
  - attribute: machine_id
    value: "{{ qc_remote_machine }}"
roles:
  - name: "internal-dpl-clock"
    connect:
    task:
      load: tpc-qc-post-calibration-internal-dpl-clock
  - name: "PP-TASK-RUNNER-PadCalibration"
    connect:
    - name: from_internal-dpl-clock_to_PP-TASK-RUNNER-PadCalibration
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_PP-TASK-RUNNER-PadCalibration"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: tpc-qc-post-calibration-PP-TASK-RUNNER-PadCalibration
  - name: "QC-CHECK-RUNNER-PadCalibrationCheck2D"
    connect:
    - name: from_PP-TASK-RUNNER-PadCalibration_to_QC-CHECK-RUNNER-PadCalibrationCheck2D
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.PP-TASK-RUNNER-PadCalibration:from_PP-TASK-RUNNER-PadCalibration_to_QC-CHECK-RUNNER-PadCalibrationCheck2D"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: tpc-qc-post-calibration-QC-CHECK-RUNNER-PadCalibrationCheck2D
  - name: "internal-dpl-injected-dummy-sink"
    connect:
    - name: from_QC-CHECK-RUNNER-PadCalibrationCheck2D_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-CHECK-RUNNER-PadCalibrationCheck2D:from_QC-CHECK-RUNNER-PadCalibrationCheck2D_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: tpc-qc-post-calibration-internal-dpl-injected-dummy-sink
  - name: fairmq-shmmonitor
    enabled: "{{fmq_cleanup_enabled == 'true'}}"
    task:
      load: "fairmq-shmmonitor"
      trigger: DESTROY
      timeout: 10s
      critical: false
